Local inputs for WF1's independent manual technical-sheet OCR demo go here.

Manufacturer PDFs are excluded from the distributable archive. On the inspected computer the three existing local PDFs were copied here. Consult `../app/technical_sheets/README.md` for their filenames. This directory is mounted read-only at `/files/technical_sheets` in n8n.
